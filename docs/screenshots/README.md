# Screenshots

Drop the app screenshots here with **exactly these filenames** so the links in the
root `README.md` render on GitHub:

| Filename | Screen |
|----------|--------|
| `overview.png` | Overview — Environment Consumption Overview |
| `infrastructure-k8s.png` | Infrastructure & K8s |
| `observability.png` | Observability |
| `applications.png` | Applications |
| `billing.png` | Billing & Cost Analysis |
| `predictions.png` | Consumption Predictions |
| `settings-rate-card.png` | Settings — Rate Card configuration |

To add them: save each PNG here with the name above and commit, or upload via the
GitHub web UI (Add file → Upload files) into `docs/screenshots/`.
